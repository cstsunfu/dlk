{
    config: {
        decoder: {
            _base: "linear",
            config: {
                input_size: 200,
                output_size: 3
            }
        },
        encoder: {
            _base: "lstm",
            config: {
                output_size: 8
            }
        }
    },
    name: "lstm_linear_ner",
}
