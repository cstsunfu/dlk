{
    config: {
        vocab_size: "*@*",
        embedding_dim: "*@*",
        dropout: 0, //dropout rate
        output_map: {},
        return_logits: "embedding_logits",
    },
    _name: "random",
}
