{
    "_base": "checkpoint",
    "config": {
        "monitor": "val_loss",
        "every_n_epochs": 1,
        "mode": "min", //"max" or "min" select topk min or max checkpoint, min for loss, max for acc
    }
}
