{
    "_base": "basic@span_cls#pretrained",
    "config": {
        "feed_order": ["load", "seq_lab_loader", "tokenizer", "label_gather", "span_cls_relabel", "save"]
        /*"tokenizer_config_path": "local_data/pretrained_model/roberta_base/tokenizer.json", // the tokenizer config path (the tokenizer.json path)*/
        "tokenizer_config_path": "./distil_bert/model/tokenizer.json", // the tokenizer config path (the tokenizer.json path)
        "data_dir": "./distil_bert/output/",  // save load data base dir
    },
}
